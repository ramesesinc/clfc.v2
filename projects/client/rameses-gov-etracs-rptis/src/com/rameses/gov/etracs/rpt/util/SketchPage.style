"uploadSketch|removeSketch" [true == true]{
    enabled:false;
}

"uploadSketch" [ (image == null || image.length == 0) && ! entity.state.matches('CURRENT|CANCELLED')]{
    enabled:true;
}

"removeSketch" [ image.length > 0 && ! entity.state.matches('CURRENT|CANCELLED')]{
    enabled:true;
}